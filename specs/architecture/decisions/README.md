# Architecture decisions

Store only durable, consequential decisions here. Copy `specs/templates/adr.md` to `ADR-NNN-short-title.md`, assign an owner, and update its status as the decision evolves.

Do not create an ADR for routine implementation choices. A Critical change may produce an ADR when its rationale must outlive the temporary working card.
