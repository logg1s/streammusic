# `<Domain>` specification

Spec-ID: `<DOMAIN>`
Owner: `<domain-owner>`
Status: current
Last-Reviewed: `<YYYY-MM-DD>`

## Purpose

Describe the business capability and what belongs inside this domain.

## Invariants

- `<rule that must always remain true>`

## Requirements

## `<DOMAIN>-001` — `<Requirement title>`

### Rule

Describe the observable behavior in plain language.

### Acceptance criteria

- `AC-<DOMAIN>-001-01`: Given `<context>`, when `<event>`, then `<observable result>`.
- `AC-<DOMAIN>-001-02`: Given `<edge case>`, when `<event>`, then `<safe outcome>`.

### Evidence

- `<test path, contract check, or manual evidence>`

## Connections

Add links to interfaces, data, dependencies, or ADRs only when they matter.
