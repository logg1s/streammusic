# `<Capability>` requirements

This optional file inherits `Spec-ID`, `Owner`, `Status`, and `Last-Reviewed` from
`../spec.md`. Use it only when the domain index reports a map hotspot.

## `<DOMAIN-001>` — `<Observable behavior>`

### Rule

`<Current rule stated without implementation detail>`

### Acceptance criteria

- `AC-<DOMAIN-001>-01`: Given `<state>`, when `<event>`, then `<observable result>`.

### Evidence

- `<Contract, test, or runtime evidence link>`
